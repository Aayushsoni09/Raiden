# Optional local chat UI — not needed for the CLI/voice investigator paths.
# Install with: pip install -r requirements.txt -r requirements-frontend.txt
# Run with: streamlit run frontend/app.py

streamlit>=1.38
